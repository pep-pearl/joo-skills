# Domain Map: auth

## Scope

Login, signup, session, permission.

## First Read

- `apps/web/src/app/auth-routes.tsx`: auth route mapping
- `apps/web/src/pages/login`: login page area
- `apps/web/src/pages/signup`: signup page area
- `packages/domains/auth`: auth domain package/API

## Keywords

login, signin, signup, auth, session, token, 로그인, 회원가입, 인증

## Staleness Triggers

- auth routes moved
- session store changed
- auth API package changed
