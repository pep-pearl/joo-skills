---
description: Use when creating or maintaining AI_INDEX, map shards, manifest, AGENTS, context navigation rules, or @ai-* headers.
globs:
  - "**/AI_INDEX.md"
  - "**/AGENTS.md"
  - "**/rules/*.md"
  - "**/.ai/indexing/**/*.md"
  - "**/.ai/indexing/**/*.json"
alwaysApply: false
---

# Joo Repo Indexing

Use sparse, factual AI navigation metadata.

`AI_INDEX.md` is a small router. Put inventories and file maps in `.ai/indexing/maps/*`.

Do not add headers to every file.

When indexing:

1. Read existing AGENTS/rules.
2. Read or create AI_INDEX router.
3. Identify entry points.
4. Generate compact map shards when useful.
5. Follow imports.
6. Generate path-first summaries.
7. Update metadata only when future agents need it.

Minimal header format:

```ts
/**
 * @ai-purpose Short responsibility.
 * @ai-domain routing | api | state | page | feature | entity | shared | config | test
 * @ai-keywords Searchable names and user-facing aliases.
 */
```

Extended fields are optional: `@ai-entry`, `@ai-depends`, `@ai-used-by`, `@ai-notes`.
