# Context navigation

Use AI_INDEX as a small router, then at most one map, then source. Stop reading metadata after a source entry point is identified. Prefer import-following over repository-wide content search.
