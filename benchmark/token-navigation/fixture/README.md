# ACME Commerce fixture

Synthetic monorepo used for repository-navigation benchmarks. Production applications live in
`apps/storefront`, `apps/admin`, and `apps/b2b`. The repository intentionally also contains legacy,
archive, generated, example, and Storybook files with overlapping names.
